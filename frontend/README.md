# Switchie Frontend

React + TypeScript + Vite frontend boundary for Switchie.

The current visual game is preserved inside `src/legacy` as a compatibility layer so the migration does not alter the existing table design or gameplay. New UI should be added as React components in `src/components`, pure game helpers in `src/game`, and reusable animations in `src/animations`.
