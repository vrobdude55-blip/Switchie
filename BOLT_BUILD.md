# Bolt build preview

`npm run build` creates `dist/` by copying the existing `public/` frontend.
The production Node.js + Express + Socket.IO server remains unchanged and is
still started with `npm start`.
